<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="darkrai.lysis.corp.aidbot">
 <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>

   <!-- <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"></application>-->
    <application
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".BurnsActivity"
            android:label="Burns"/>
        <activity android:name=".AccidentActivity"
            android:label="Accidents"/>
        <activity android:name=".SuffocationActivity"
            android:label="Suffocation"/>
        <activity android:name=".FrostActivity"
            android:label="FrostBite"/>
        <activity android:name=".FaintActivity"
            android:label="Fainting"/>
        <activity android:name=".ShockActivity"
            android:label="Shock"></activity>
    </application>

</manifest>
    <!--android:roundIcon="@mipmap/ic_launcher_round"-->